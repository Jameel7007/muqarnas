{
  "name": "@muqarnas/site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5175 --strictPort",
    "build": "vite build",
    "prebake": "tsx scripts/prebake.ts",
    "preview": "vite preview --port 5175 --strictPort",
    "deploy": "npm run prebake && vite build && gh-pages -d dist"
  },
  "dependencies": {
    "@muqarnas/lift": "*",
    "@muqarnas/plan": "*",
    "@muqarnas/render": "*",
    "@muqarnas/scenes": "*",
    "lenis": "^1.3.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
