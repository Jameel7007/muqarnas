{
  "name": "@muqarnas/site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5175 --strictPort",
    "build": "npm run tallies && vite build",
    "prebake": "tsx scripts/prebake.ts",
    "preview": "vite preview --port 5175 --strictPort",
    "deploy": "npm run prebake && npm run build && gh-pages -d dist",
    "tallies": "tsx scripts/tallies.ts"
  },
  "dependencies": {
    "@muqarnas/lift": "*",
    "@muqarnas/plan": "*",
    "@muqarnas/render": "*",
    "@muqarnas/scenes": "*",
    "lenis": "^1.3.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
